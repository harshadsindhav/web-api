import { SearchResultDecoratorDirective } from './search-result-decorator.directive';

describe('SearchResultDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchResultDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
