import { HttpMethodDirective } from './http-method.directive';

describe('HttpMethodDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpMethodDirective();
    expect(directive).toBeTruthy();
  });
});
